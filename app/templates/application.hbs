{{console-time "render_ended"}}
<table>
<tbody>
  {{#each this.items as |row index|}}
    <TR @row={{row}} @rowIndex={{rowIndex}} />
  {{/each}}
  </tbody>
  </table>
{{console-time-end "render_ended"}}
